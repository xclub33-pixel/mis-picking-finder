services:
  - type: web
    name: mis-picking-finder
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: EXCEL_FILE
        value: 사용자정의주문현황.xlsx
